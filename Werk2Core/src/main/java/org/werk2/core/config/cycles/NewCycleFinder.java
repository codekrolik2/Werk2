package org.werk2.core.config.cycles;

public class NewCycleFinder {
	//*) All functions without engine and with engine
	//Engine -> Flows -> Steps -> Exec, Transit

	//* Building a mermaid call graph
	
	//All functions with engine, (show warnings instead)
	
	
}
